<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.adminDaoMapper">
	
	
	<select id="getManageTeamCount" resultType="int">
		<![CDATA[
		select count(*) from team
		]]>
	</select>	
	
	<select id="getManageTeam" parameterType="java.util.HashMap" resultType="java.util.HashMap">
		<![CDATA[
		select * from
		(select rownum as rnum, c.* from
			(select a.emblem,a.teamname,a.teamleadername, b.* from team a, teamMember b 
				where a.teamcode=b.teamcode) c)
					where rnum>=#{startRow} and rnum<=#{endRow}
		]]>
	
	</select>
	
	<delete id="manageTeamDelete" parameterType="int">
		DELETE FROM 
          (SELECT * 
            FROM team ,teammember
            WHERE team.teamcode = teammember.teamcode and team.teamcode=#{teamcode}
          )
	</delete>
	
</mapper>